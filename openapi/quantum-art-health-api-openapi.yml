openapi: 3.1.0
info:
  title: QaaS Backend admin Health API
  description: Quantum-as-a-Service Backend providing task management, metrics, and real-time communication
  version: 1.0.0
tags:
- name: Health
paths:
  /health:
    get:
      summary: Health Check
      description: Minimal health check for ALB probes. Returns only overall status.
      operationId: health_check_health_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
      tags:
      - Health
components:
  securitySchemes:
    HTTPBearer:
      type: http
      scheme: bearer
